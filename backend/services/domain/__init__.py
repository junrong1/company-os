"""The domain service: the metric and effect model behind the origin R15 seam.

Phase 1 ships the flat authored deltas the prototype already has. Phase 4 replaces
the implementation without touching the contract.
"""
